import React from "react"
import { View, Text, FlatList } from "react-native"
import UI from "@app-components/UI"

export default function ProjectsList() {
  return (
    <UI.Card>
      <UI.Card.Title>React Fanmade Hooks</UI.Card.Title>
    </UI.Card>
  )
}
